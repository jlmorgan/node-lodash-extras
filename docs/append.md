# _.append(appendix, initial)

Append the `appendix` string to the `initial` string.

## Arguments

* `appendix (String)`: The appendix string.
* `initial (String)`: The initial string.

## Returns

* `(String)`: The appended string.

## Examples

### Append strings

```javascript
_.append("b", "a");
// => "ab"
```
